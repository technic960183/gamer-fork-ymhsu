# problem-specific runtime parameters (Sec 4.1.5: 2D anisotropic diffusion along circular B field lines)
# NOTE: the IC hardcodes rho = 1e8 to emulate the paper's "MHD variables fixed" -- the circular B has an
#       unbalanced hoop stress that would otherwise implode the gas and compress a spurious CR peak at r=0
CR_Streaming_Test        3              # 3 = 2D ring Ec, circular B
CR_Streaming_Dir         0              # unused for the 2D tests
CR_Streaming_FlowV       0.0
